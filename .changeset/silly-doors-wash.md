---
---

Docs-only: README.md gained a pointer to `docs/design/CONVENTION.md` (a worked example of
using `checks.coverage`'s existing kinds/rules for design-package structural enforcement).
No change to the published package's runtime behavior — empty changeset, no version bump.
